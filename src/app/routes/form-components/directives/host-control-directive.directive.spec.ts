import { HostControlDirectiveDirective } from './host-control-directive.directive';

describe('HostControlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HostControlDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
